# Shared Utils
Collection of shared utilities, helpers, and constants used across the monorepo.

## Modules
- **Config**: Global configuration management (Env vars, app settings, security, flags).
- **Errors**: Standardized error handling classes.
